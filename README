
To get running without Apache:

   cd shellinabox-2.10/
   ./configure
   make; make install

   ./start-siab.sh

You can then access scripts by name of the folder
in replays/ with the following url pattern

   http://localhost:4200/?test